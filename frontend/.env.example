# Backend API base URL (no trailing slash).
# Production (Render):
VITE_API_URL=https://ktu-backend.onrender.com
# Local dev, uncomment to point at a locally running backend:
# VITE_API_URL=http://127.0.0.1:8000
